
(function(l, r) { if (l.getElementById('livereloadscript')) return; r = l.createElement('script'); r.async = 1; r.src = '//' + (window.location.host || 'localhost').split(':')[0] + ':35729/livereload.js?snipver=1'; r.id = 'livereloadscript'; l.getElementsByTagName('head')[0].appendChild(r) })(window.document);
import { S as SvelteComponentDev, i as init, d as dispatch_dev, s as safe_not_equal, v as validate_slots, e as element, a as attr_dev, b as add_location, c as insert_dev, f as add_render_callback, g as create_bidirectional_transition, h as detach_dev, j as space, k as append_dev, l as listen_dev, t as transition_in, m as transition_out, n as check_outros, r as run_all, o as group_outros } from './main2.js';
import { f as fade } from './index.js';

/* src\routes\Bridge.svelte generated by Svelte v3.38.2 */
const file = "src\\routes\\Bridge.svelte";

// (26:4) {#if show == 1}
function create_if_block_3(ctx) {
	let img;
	let img_src_value;
	let img_transition;
	let current;

	const block = {
		c: function create() {
			img = element("img");
			attr_dev(img, "class", "content-img svelte-oao615");
			if (img.src !== (img_src_value = "images/blue-eyed-cat-peeking.jpg")) attr_dev(img, "src", img_src_value);
			attr_dev(img, "alt", "blue eyed cat peeking");
			add_location(img, file, 26, 6, 696);
		},
		m: function mount(target, anchor) {
			insert_dev(target, img, anchor);
			current = true;
		},
		i: function intro(local) {
			if (current) return;

			add_render_callback(() => {
				if (!img_transition) img_transition = create_bidirectional_transition(img, fade, {}, true);
				img_transition.run(1);
			});

			current = true;
		},
		o: function outro(local) {
			if (!img_transition) img_transition = create_bidirectional_transition(img, fade, {}, false);
			img_transition.run(0);
			current = false;
		},
		d: function destroy(detaching) {
			if (detaching) detach_dev(img);
			if (detaching && img_transition) img_transition.end();
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_if_block_3.name,
		type: "if",
		source: "(26:4) {#if show == 1}",
		ctx
	});

	return block;
}

// (34:4) {#if show == 2}
function create_if_block_2(ctx) {
	let img;
	let img_src_value;
	let img_transition;
	let current;

	const block = {
		c: function create() {
			img = element("img");
			attr_dev(img, "class", "content-img svelte-oao615");
			if (img.src !== (img_src_value = "images/cute-curious-cat.jpg")) attr_dev(img, "src", img_src_value);
			attr_dev(img, "alt", "cute curious cat");
			add_location(img, file, 34, 6, 889);
		},
		m: function mount(target, anchor) {
			insert_dev(target, img, anchor);
			current = true;
		},
		i: function intro(local) {
			if (current) return;

			add_render_callback(() => {
				if (!img_transition) img_transition = create_bidirectional_transition(img, fade, {}, true);
				img_transition.run(1);
			});

			current = true;
		},
		o: function outro(local) {
			if (!img_transition) img_transition = create_bidirectional_transition(img, fade, {}, false);
			img_transition.run(0);
			current = false;
		},
		d: function destroy(detaching) {
			if (detaching) detach_dev(img);
			if (detaching && img_transition) img_transition.end();
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_if_block_2.name,
		type: "if",
		source: "(34:4) {#if show == 2}",
		ctx
	});

	return block;
}

// (42:4) {#if show == 3}
function create_if_block_1(ctx) {
	let img;
	let img_src_value;
	let img_transition;
	let current;

	const block = {
		c: function create() {
			img = element("img");
			attr_dev(img, "class", "content-img svelte-oao615");
			if (img.src !== (img_src_value = "images/lion-laying-on-rock.jpg")) attr_dev(img, "src", img_src_value);
			attr_dev(img, "alt", "lion laying on rock");
			add_location(img, file, 42, 6, 1072);
		},
		m: function mount(target, anchor) {
			insert_dev(target, img, anchor);
			current = true;
		},
		i: function intro(local) {
			if (current) return;

			add_render_callback(() => {
				if (!img_transition) img_transition = create_bidirectional_transition(img, fade, {}, true);
				img_transition.run(1);
			});

			current = true;
		},
		o: function outro(local) {
			if (!img_transition) img_transition = create_bidirectional_transition(img, fade, {}, false);
			img_transition.run(0);
			current = false;
		},
		d: function destroy(detaching) {
			if (detaching) detach_dev(img);
			if (detaching && img_transition) img_transition.end();
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_if_block_1.name,
		type: "if",
		source: "(42:4) {#if show == 3}",
		ctx
	});

	return block;
}

// (50:4) {#if show == 4}
function create_if_block(ctx) {
	let img;
	let img_src_value;
	let img_transition;
	let current;

	const block = {
		c: function create() {
			img = element("img");
			attr_dev(img, "class", "content-img svelte-oao615");
			if (img.src !== (img_src_value = "images/cat-with-big-ears.jpg")) attr_dev(img, "src", img_src_value);
			attr_dev(img, "alt", "cat with big ears");
			add_location(img, file, 50, 6, 1261);
		},
		m: function mount(target, anchor) {
			insert_dev(target, img, anchor);
			current = true;
		},
		i: function intro(local) {
			if (current) return;

			add_render_callback(() => {
				if (!img_transition) img_transition = create_bidirectional_transition(img, fade, {}, true);
				img_transition.run(1);
			});

			current = true;
		},
		o: function outro(local) {
			if (!img_transition) img_transition = create_bidirectional_transition(img, fade, {}, false);
			img_transition.run(0);
			current = false;
		},
		d: function destroy(detaching) {
			if (detaching) detach_dev(img);
			if (detaching && img_transition) img_transition.end();
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_if_block.name,
		type: "if",
		source: "(50:4) {#if show == 4}",
		ctx
	});

	return block;
}

function create_fragment(ctx) {
	let main;
	let div0;
	let button0;
	let t1;
	let button1;
	let t3;
	let button2;
	let t5;
	let button3;
	let t7;
	let div1;
	let t8;
	let t9;
	let t10;
	let current;
	let mounted;
	let dispose;
	let if_block0 = /*show*/ ctx[0] == 1 && create_if_block_3(ctx);
	let if_block1 = /*show*/ ctx[0] == 2 && create_if_block_2(ctx);
	let if_block2 = /*show*/ ctx[0] == 3 && create_if_block_1(ctx);
	let if_block3 = /*show*/ ctx[0] == 4 && create_if_block(ctx);

	const block = {
		c: function create() {
			main = element("main");
			div0 = element("div");
			button0 = element("button");
			button0.textContent = "Existing View";
			t1 = space();
			button1 = element("button");
			button1.textContent = "Day 1 without Mitigation Measures";
			t3 = space();
			button2 = element("button");
			button2.textContent = "Day 1 with Mitigation Measures";
			t5 = space();
			button3 = element("button");
			button3.textContent = "Year 10 with Mitigation Measures";
			t7 = space();
			div1 = element("div");
			if (if_block0) if_block0.c();
			t8 = space();
			if (if_block1) if_block1.c();
			t9 = space();
			if (if_block2) if_block2.c();
			t10 = space();
			if (if_block3) if_block3.c();
			attr_dev(button0, "class", "nav-btn svelte-oao615");
			add_location(button0, file, 11, 4, 182);
			attr_dev(button1, "class", "nav-btn svelte-oao615");
			add_location(button1, file, 14, 4, 282);
			attr_dev(button2, "class", "nav-btn svelte-oao615");
			add_location(button2, file, 17, 4, 402);
			attr_dev(button3, "class", "nav-btn svelte-oao615");
			add_location(button3, file, 20, 4, 519);
			attr_dev(div0, "class", "nav svelte-oao615");
			add_location(div0, file, 10, 2, 159);
			attr_dev(div1, "class", "content svelte-oao615");
			add_location(div1, file, 24, 2, 646);
			attr_dev(main, "class", "svelte-oao615");
			add_location(main, file, 9, 0, 149);
		},
		l: function claim(nodes) {
			throw new Error("options.hydrate only works if the component was compiled with the `hydratable: true` option");
		},
		m: function mount(target, anchor) {
			insert_dev(target, main, anchor);
			append_dev(main, div0);
			append_dev(div0, button0);
			append_dev(div0, t1);
			append_dev(div0, button1);
			append_dev(div0, t3);
			append_dev(div0, button2);
			append_dev(div0, t5);
			append_dev(div0, button3);
			append_dev(main, t7);
			append_dev(main, div1);
			if (if_block0) if_block0.m(div1, null);
			append_dev(div1, t8);
			if (if_block1) if_block1.m(div1, null);
			append_dev(div1, t9);
			if (if_block2) if_block2.m(div1, null);
			append_dev(div1, t10);
			if (if_block3) if_block3.m(div1, null);
			current = true;

			if (!mounted) {
				dispose = [
					listen_dev(button0, "click", /*click_handler*/ ctx[2], false, false, false),
					listen_dev(button1, "click", /*click_handler_1*/ ctx[3], false, false, false),
					listen_dev(button2, "click", /*click_handler_2*/ ctx[4], false, false, false),
					listen_dev(button3, "click", /*click_handler_3*/ ctx[5], false, false, false)
				];

				mounted = true;
			}
		},
		p: function update(ctx, [dirty]) {
			if (/*show*/ ctx[0] == 1) {
				if (if_block0) {
					if (dirty & /*show*/ 1) {
						transition_in(if_block0, 1);
					}
				} else {
					if_block0 = create_if_block_3(ctx);
					if_block0.c();
					transition_in(if_block0, 1);
					if_block0.m(div1, t8);
				}
			} else if (if_block0) {
				group_outros();

				transition_out(if_block0, 1, 1, () => {
					if_block0 = null;
				});

				check_outros();
			}

			if (/*show*/ ctx[0] == 2) {
				if (if_block1) {
					if (dirty & /*show*/ 1) {
						transition_in(if_block1, 1);
					}
				} else {
					if_block1 = create_if_block_2(ctx);
					if_block1.c();
					transition_in(if_block1, 1);
					if_block1.m(div1, t9);
				}
			} else if (if_block1) {
				group_outros();

				transition_out(if_block1, 1, 1, () => {
					if_block1 = null;
				});

				check_outros();
			}

			if (/*show*/ ctx[0] == 3) {
				if (if_block2) {
					if (dirty & /*show*/ 1) {
						transition_in(if_block2, 1);
					}
				} else {
					if_block2 = create_if_block_1(ctx);
					if_block2.c();
					transition_in(if_block2, 1);
					if_block2.m(div1, t10);
				}
			} else if (if_block2) {
				group_outros();

				transition_out(if_block2, 1, 1, () => {
					if_block2 = null;
				});

				check_outros();
			}

			if (/*show*/ ctx[0] == 4) {
				if (if_block3) {
					if (dirty & /*show*/ 1) {
						transition_in(if_block3, 1);
					}
				} else {
					if_block3 = create_if_block(ctx);
					if_block3.c();
					transition_in(if_block3, 1);
					if_block3.m(div1, null);
				}
			} else if (if_block3) {
				group_outros();

				transition_out(if_block3, 1, 1, () => {
					if_block3 = null;
				});

				check_outros();
			}
		},
		i: function intro(local) {
			if (current) return;
			transition_in(if_block0);
			transition_in(if_block1);
			transition_in(if_block2);
			transition_in(if_block3);
			current = true;
		},
		o: function outro(local) {
			transition_out(if_block0);
			transition_out(if_block1);
			transition_out(if_block2);
			transition_out(if_block3);
			current = false;
		},
		d: function destroy(detaching) {
			if (detaching) detach_dev(main);
			if (if_block0) if_block0.d();
			if (if_block1) if_block1.d();
			if (if_block2) if_block2.d();
			if (if_block3) if_block3.d();
			mounted = false;
			run_all(dispose);
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_fragment.name,
		type: "component",
		source: "",
		ctx
	});

	return block;
}

function instance($$self, $$props, $$invalidate) {
	let { $$slots: slots = {}, $$scope } = $$props;
	validate_slots("Bridge", slots, []);
	let { show } = $$props;

	function switchContent(index) {
		$$invalidate(0, show = index);
	}

	const writable_props = ["show"];

	Object.keys($$props).forEach(key => {
		if (!~writable_props.indexOf(key) && key.slice(0, 2) !== "$$") console.warn(`<Bridge> was created with unknown prop '${key}'`);
	});

	const click_handler = () => switchContent(1);
	const click_handler_1 = () => switchContent(2);
	const click_handler_2 = () => switchContent(3);
	const click_handler_3 = () => switchContent(4);

	$$self.$$set = $$props => {
		if ("show" in $$props) $$invalidate(0, show = $$props.show);
	};

	$$self.$capture_state = () => ({ fade, show, switchContent });

	$$self.$inject_state = $$props => {
		if ("show" in $$props) $$invalidate(0, show = $$props.show);
	};

	if ($$props && "$$inject" in $$props) {
		$$self.$inject_state($$props.$$inject);
	}

	return [
		show,
		switchContent,
		click_handler,
		click_handler_1,
		click_handler_2,
		click_handler_3
	];
}

class Bridge extends SvelteComponentDev {
	constructor(options) {
		super(options);
		init(this, options, instance, create_fragment, safe_not_equal, { show: 0 });

		dispatch_dev("SvelteRegisterComponent", {
			component: this,
			tagName: "Bridge",
			options,
			id: create_fragment.name
		});

		const { ctx } = this.$$;
		const props = options.props || {};

		if (/*show*/ ctx[0] === undefined && !("show" in props)) {
			console.warn("<Bridge> was created without expected prop 'show'");
		}
	}

	get show() {
		throw new Error("<Bridge>: Props cannot be read directly from the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	set show(value) {
		throw new Error("<Bridge>: Props cannot be set directly on the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}
}

export default Bridge;
//# sourceMappingURL=Bridge.js.map
